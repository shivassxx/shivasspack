# Copy to .env.migrations (ignored). Use a separately generated password.
# Never copy this URL into DATABASE_URL or the application's container environment.
DATABASE_MIGRATION_URL=postgresql://shivass_migrator:replace_with_random_hex@127.0.0.1:5432/shivasspack
